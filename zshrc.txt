# orami's Cyberpunk ZSH Config for Arch Linux

source ~/powerlevel10k/powerlevel10k.zsh-theme

if command -v fastfetch &>/dev/null; then
  fastfetch --logo color --color-keys magenta --color-title cyan
fi

if command -v figlet &>/dev/null && command -v lolcat &>/dev/null; then
  figlet -f slant "Welcome back, Neo" | lolcat
fi

source ~/.zsh/zsh-autosuggestions/zsh-autosuggestions.zsh
source ~/.zsh/zsh-syntax-highlighting/zsh-syntax-highlighting.zsh

alias ls='exa -al --color=always --group-directories-first'
alias la='exa -a'
alias ll='exa -l'
alias lt='exa -aT --icons'
alias tree='exa -T --icons'
alias cls='clear'

alias update='sudo pacman -Syu'
alias ports='netstat -tulanp'
alias ipinfo='ip a'
alias myip='curl ifconfig.me'
alias pyserve='python3 -m http.server'
alias scan='nmap -sV -Pn'
alias gs='git status'
alias gc='git commit -m'
alias ..='cd ..'
alias ...='cd ../..'
alias venv="python3 -m venv .venv && source .venv/bin/activate"
alias reload='source ~/.zshrc'
alias linuxdoc='w3m https://tldp.org/'
alias archwiki='w3m https://wiki.archlinux.org/title/Main_page'

HISTFILE=~/.zsh_history
HISTSIZE=10000
SAVEHIST=10000
setopt INC_APPEND_HISTORY
setopt HIST_IGNORE_ALL_DUPS
setopt HIST_REDUCE_BLANKS
setopt AUTO_CD

[[ ! -f ~/.p10k.zsh ]] || source ~/.p10k.zsh

autoload -Uz compinit && compinit
zle -N autosuggest-accept
bindkey '^ ' autosuggest-accept

# echo "%F{magenta}orami infosec%f"

## [Completion]
## Completion scripts setup. Remove the following line to uninstall
[[ -f /home/orami/.dart-cli-completion/zsh-config.zsh ]] && . /home/orami/.dart-cli-completion/zsh-config.zsh || true
## [/Completion]
